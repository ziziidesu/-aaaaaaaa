services:
  - type: web
    name: aaaatube
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node src/server.js
    branch: main
    envVars:
      - key: NODE_ENV
        value: production
  

services:
  - type: static_site  # 静的サイトとして正しいタイプ
    name: YouTubeStreamPlayer  # サービスの名前
    env: static  # 環境は静的サイト用
    staticPublishPath: /  # 公開パスの指定
    buildCommand: ""  # ビルドコマンドは不要
    publishPath: .  # 現在のディレクトリを公開

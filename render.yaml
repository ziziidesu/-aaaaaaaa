services:
  - type: web
    name: aaaatube
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    branch: main
    envVars:
      - key: NODE_ENV
        value: production
    # 必要であればポート番号を指定（Render では通常 10000 番ポートを使う）
    port: 10000
